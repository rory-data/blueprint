name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    uses: ./.github/workflows/lint-reusable.yml

  test:
    uses: ./.github/workflows/test-reusable.yml
    with:
      python-version: "3.12"
